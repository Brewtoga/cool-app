version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist    # Vite builds to 'dist' folder
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*